version: "3.9"

services:

  sistema:

    build: .

    container_name: sistema_chamados

    restart: always

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - .:/app

    command: python app.py
